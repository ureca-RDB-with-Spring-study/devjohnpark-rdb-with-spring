spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  flyway:
    baseline-on-migrate: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true